if flash
  .alert.alert-dismissible.show(role='alert' class=flash.type === 'error' ? 'alert-danger' : 'alert-success')
    strong #{flash.message}
    button.close(type='button' data-dismiss='alert' aria-label='Close')
      span(aria-hidden='true') ×
