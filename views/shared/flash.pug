if errorMessage
  .alert.alert-dismissible.show(role='alert' class=errorMessage.type === 'error' ? 'alert-danger' : 'alert-success')
    strong #{errorMessage.message}
    button.close(type='button' data-dismiss='alert' aria-label='Close')
      span(aria-hidden='true') ×
